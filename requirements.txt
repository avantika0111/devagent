# Core
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
pyyaml>=6.0
python-dotenv>=1.0.0

# Model providers (OpenAI-compatible client used for both NIM and Gemini)
openai>=1.30.0

# GitHub
PyGithub>=2.3.0

# Security scanning (Phase 4)
semgrep>=1.68.0
pip-audit>=2.7.0
bandit>=1.7.8

# Code analysis (Phase 3)
coverage>=7.4.0
pytest>=8.1.0

# Docker (Phase 4)
docker>=7.0.0

# CLI (Phase 5)
typer>=0.12.0
rich>=13.7.0

# Dev / testing
pytest-asyncio>=0.23.0
httpx>=0.27.0
